# 2026-09-05

- 按 1.0 文档包实现一期 S0—S1 本地开发版，代码在 `code/`（pnpm 单仓库，git 已提交 8fbea17）。
- 结构：packages/contracts（payload 判别联合）、packages/domain（F01—F09 校验，Vitest 25 测试通过）、apps/web（React18+Vite，:5175）。
- 功能：手机分组日表/电脑日表、全字段编辑（血糖 7 时点+临时、血压成对反常确认、体重、三餐、饮水 0 合法、运动、胰岛素剂量须带单位、纪要）、IndexedDB 原子写草稿/记录、补记横幅、未来日期禁存、软删除 30 天恢复、字号 18/22/26 默认 22、日明细/月表（实际天数）/趋势（同单位同时点、缺失不连线）、虚构示例数据载入。
- 边界：云端同步/Excel/PWA/备份未做；UI 显示「本地版·数据仅存于此设备」；CloudBase 验证未做。
- 启动：双击 `code/启动.command` 或 `cd code && pnpm dev`。
- 坑：pnpm install 偶发 symlink 竞态，删 node_modules 重装即可；9 月有 30 天（测试写错过）。
- 进展.md 已更新。
- Git/GitHub：git 2.39.5 已有；gh v2.100.0 装到 ~/.local/gh-cli/bin（.zshrc 已加 PATH）。OAuth 设备码流程完成登录（账号 Whisky53），gh auth setup-git 配置凭据。公开仓库 https://github.com/Whisky53/elderly-glucose-log 已推送 main（commit 8fbea17）。坑：~/.gitconfig.lock 残留会阻塞 config 写入，rm 即可；gh repo create --push 与手动 remote add 冲突，分开操作。
- UI 重构（MyTurn 棋盘风适老化）：周横向日期栏、棋盘马卡龙大卡片（粗黑轮廓 SVG 图标 Icons.tsx）、填写改为卡内展开（EntryForm inline/page 两形态，EntryEditor 已删）、底部 4 项导航（填写/回看/趋势/设置）、少动效高对比。构建+25 单测通过，commit 63b8793 本地已提交；沙箱代理故障（HTTP2 framing/CONNECT 502）推送被阻，待网络恢复后 git push。
- 交互二轮改造（commit f96561f，本地）：①填写从卡内展开改为弹窗（EntryForm 变体 page/modal，DayPage 与 App.tsx 编辑入口均走 .modal-overlay/.modal-card，Esc/点遮罩关闭，背景滚动锁定）；②血糖/胰岛素的"实际测量时间"新记录默认当前时间（defaultOccurredAt = 所选日期T当前时分，标签去掉"可空"）；③布局：血糖卡 2/3 宽（.board-top 2fr 1fr），血压+体重在右 1/3 列上下堆叠（.board-side），其余 8 卡在下方 .board 流式排列。<430px 时 board-top 降级单列。tsc+build+25 单测通过。
- 修复（commit 5e20ffb，已推送）：上一轮多个 Edit 报成功但未落盘（styles.css 缺 .modal-overlay/.modal-card/.board-top/.board-side、EntryForm 缺 modal 头部与 occurredAt 默认值、domain 标签未改），导致用户看到"无弹窗、布局不变"。已全部补齐并实机验证：playwright-core + 系统 Chrome（--no-sandbox）脚本量测——血糖/血压宽度比恰 2.00、board-side 2 卡堆叠、弹窗 fixed z60 可开可 Esc 关、occurredAt 默认当前时分、控制台零报错。验证脚本 /tmp/gms-verify.js 可复用。index.html 加了内联 SVG favicon 消除 404。GitHub 推送恢复，8fbea17..5e20ffb 已上远端。教训：Edit 报成功不等于落盘，批量改动后必须 grep 复核关键标记再进入验证。
- 三轮微调（5e20ffb..0c577e1 已推送）：①单卡按钮"点这里填写"→"填写"；②胰岛素卡移入右列（血压→体重→胰岛素，board-side 3 卡）；③"一日纪要"全局更名"日纪要"（fields/validation/测试/DayPage/HistoryPage/README）。坑：macOS sed 处理中文多字节报 "No such file or directory"，用 perl -pi -e 替代。每轮均跑 /tmp/gms-verify.js 实测（2:1 比例、弹窗、默认时间、零报错）后才交付。沙箱代理对 github.com 间歇 502，重试（sleep 60-90）可恢复。
