# 2026-09-05

- 按 1.0 文档包实现一期 S0—S1 本地开发版，代码在 `code/`（pnpm 单仓库，git 已提交 8fbea17）。
- 结构：packages/contracts（payload 判别联合）、packages/domain（F01—F09 校验，Vitest 25 测试通过）、apps/web（React18+Vite，:5175）。
- 功能：手机分组日表/电脑日表、全字段编辑（血糖 7 时点+临时、血压成对反常确认、体重、三餐、饮水 0 合法、运动、胰岛素剂量须带单位、纪要）、IndexedDB 原子写草稿/记录、补记横幅、未来日期禁存、软删除 30 天恢复、字号 18/22/26 默认 22、日明细/月表（实际天数）/趋势（同单位同时点、缺失不连线）、虚构示例数据载入。
- 边界：云端同步/Excel/PWA/备份未做；UI 显示「本地版·数据仅存于此设备」；CloudBase 验证未做。
- 启动：双击 `code/启动.command` 或 `cd code && pnpm dev`。
- 坑：pnpm install 偶发 symlink 竞态，删 node_modules 重装即可；9 月有 30 天（测试写错过）。
- 进展.md 已更新。
- Git/GitHub：git 2.39.5 已有；gh v2.100.0 装到 ~/.local/gh-cli/bin（.zshrc 已加 PATH）。OAuth 设备码流程完成登录（账号 Whisky53），gh auth setup-git 配置凭据。公开仓库 https://github.com/Whisky53/elderly-glucose-log 已推送 main（commit 8fbea17）。坑：~/.gitconfig.lock 残留会阻塞 config 写入，rm 即可；gh repo create --push 与手动 remote add 冲突，分开操作。
- UI 重构（MyTurn 棋盘风适老化）：周横向日期栏、棋盘马卡龙大卡片（粗黑轮廓 SVG 图标 Icons.tsx）、填写改为卡内展开（EntryForm inline/page 两形态，EntryEditor 已删）、底部 4 项导航（填写/回看/趋势/设置）、少动效高对比。构建+25 单测通过，commit 63b8793 本地已提交；沙箱代理故障（HTTP2 framing/CONNECT 502）推送被阻，待网络恢复后 git push。
