{
  "name": "glucose-management",
  "private": true,
  "version": "0.1.0",
  "description": "老年人血糖管理软件 R1 本地开发版（S0—S1：工程骨架 + 字段契约 + 日表填写，云端同步未接入）",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter web dev"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
